#!/bin/bash

brew install git rbenv ruby-build pyenv pyenv-pip-rehash readline node ctags watchman tmux the_silver_searcher reattach-to-user-namespace postgresql

pip install awscli

npm install bower -g
npm install ember-cli -g
